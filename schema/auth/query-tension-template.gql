{ or: [
    # Authorize root
    { rule: "{ $USERTYPE: {eq: \"Root\"} }" },
    # Authorize public data
    { rule: """query {
        queryTensionTemplate {
          nodes(filter: {visibility: {eq: Public}}) { id }
        }
    }""" },
    # Authorize members
    { rule: """query ($ROOTIDS: [String]) {
        queryTensionTemplate(filter: {rootnameid: {in: $ROOTIDS}}) { id }
    }""" }
  ]
}
